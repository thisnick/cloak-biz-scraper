# Secrets — THIS REPO IS PUBLIC. Never commit real credentials.
.env
.env.*
!.env.example

# Local runtime state
data/
.dek

# Agent scratch: reviewer worktrees live here and are their own checkouts.
.claude/worktrees/

# Local handoff/working notes — internal ops detail, keep out of the public repo
HANDOFF.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
site/

# Devbox's generated Nix profile and project-local npm packages. Keep
# devbox.json/devbox.lock tracked; they are the reproducible inputs.
.devbox/

# OS
.DS_Store
