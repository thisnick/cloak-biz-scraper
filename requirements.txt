fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
# The settings UI: server-rendered templates and form parsing. No JS toolchain.
jinja2==3.1.6
python-multipart==0.0.20
# Notion API + measuring the proxy's exit IP.
httpx==0.28.1
# [geoip] pulls geoip2 — without it, geoip=True raises at launch and the browser's
# timezone would contradict its exit IP.
cloakbrowser[geoip]==0.4.8
cryptography==44.0.1
