[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    real_startup_resolve: let the lifespan run its real build-resolution hop (see conftest)
